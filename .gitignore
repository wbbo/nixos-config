# 只追踪项目核心文件
*
!*/
!.envrc
!.gitattributes
!.gitignore
!*.kdl
!*.lock
!*.md
!*.nix
!*.sh
!*.terminfo
!*.toml
!*.py
!*.svg
!*.conf
!.sops.yaml
!*.yaml
!hosts/default/hardware-configuration.nix
# 本地定制文件不入库 (每台机器自己的 hostName/mainUser 等, 类似 secrets 的本地化)
# secrets/secrets.yaml 为 sops 加密密文, 直接签入 git (Nix flake 读取需要文件被追踪)。
# 接收者: 用 secrets/secrets.template.yaml 重建 → 填真实值 → 用自己的 age 公钥重加密
# 覆盖后 commit 到自己的 fork (勿向原模板仓库回推自己的密文)。
.claude/
doc/
config.yaml
/mihomo
/NyxNiri

